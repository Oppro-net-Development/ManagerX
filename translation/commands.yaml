de:
  # ────────────── TEMPVC ──────────────
  tempvc:
    

    create:
      name: erstellen
      description: Erstelle ein VC-Erstellungssystem
      options:
        creator_channel: 
          name: kanal
          description: Kanal, den Mitglieder betreten, um ihren VC zu erstellen

        category:
          name: kategorie
          description: Kategorie, in der die Temp-Channels erstellt werden

    remove:
      name: entferne
      description: Entferne das VC-Erstellungssystem


    settings:
      name: einstellung
      description: Zeige die aktuellen Temp-VC Einstellungen

  # ────────────── WECOME ──────────────
  welcome:
    name: willkommen
    description: Willkommensystem Einstellungen

    channel:
      name: kanal
      description: Setzt den Willkommens Kanal
      options:
        channel:
          name: kanal
          
    message:
      name: nachricht
      description: Setzt die Willkommens Nachricht
    
    toggle:
      description: Schaltet das Willkommensystem ein/aus 

    embed:
      description: Aktiviert/Deaktiviert Embed Modus

    autorole:
      description: Setzt eine Rolle die automatisch vergeben wird

    dm:
      description: Aktiviert/Konfiguriert private Willkommensnachrichten
      options:
        message:
          name: nachricht

    template:
      name: vorlage
      description: Lädt eine Vorlage
      options:
        template_name: 
          name: vorlage_name
          
    config:
      name: konfiguration
      description: Zeigt die aktuelle konfiguration
    
    test:
      name: test
      description: Testet die willkommens Nachricht

    placeholders:
      name: platzhalter
      description: Zeigt alle verfügbaren Platzhalter

    export:
      description: Exportiert die Willkommen Konfiguration

    stats:
      name: stats
      description: Zeigt die Willkommens Statistiken

    reset:
      description: Setzt alle Welcome Einstellungen zurück
    


# ────────────── STATS ──────────────
# ────────────── ENGLISH ──────────────

en-US:
  # ────────────── TEMPVC ──────────────
  tempvc:

    create:
      name: create
      description: Create a VC creation system
      options:
        creator_channel:
          name: channel
          description: Channel that members join to create their VC

        category:
          name: category
          description: Category where the temp channels will be created

    remove:
      name: remove
      description: Remove the VC creation system

    settings:
      name: settings
      description: Show the current temp VC settings

# ────────────── WELCOME ──────────────
  welcome:
    name: welcome
    description: Welcome system settings

    channel:
      name: channel
      description: Sets the welcome channel
      options:
        channel:
          name: channel
        
    message:
      name: message
      description: Sets the welcome message
  
    toggle:
      description: Enables/disables the welcome system

    embed:
      description: Enables/disables embed mode

    autorole:
      description: Sets a role that will be automatically assigned

    dm:
      description: Enables/configures private welcome messages
      options:
        message:
          name: message

    template:
      name: template
      description: Loads a template
      options:
        template_name: 
          name: template_name
        
    config:
      name: config
      description: Shows the current configuration
  
    test:
      name: test
      description: Tests the welcome message

    placeholders:
      name: placeholders
      description: Shows all available placeholders

    export:
      description: Exports the welcome configuration

    stats:
      name: stats
      description: Shows welcome statistics

    reset:
      description: Resets all welcome settings

# ────────────── STATS ──────────────